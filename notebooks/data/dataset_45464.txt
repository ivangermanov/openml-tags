Timing Attack Dataset with Thirty-Two Micro Seconds Delay Created on September First, Two Thousand Twenty-Two

Tags: Health, Medicine

Bleichenbacher Timing Attack: This dataset was created on September First, Two Thousand Twenty-Two, and it contains thirty-two micro seconds delay.

Attribute Descriptions:

The Change Cipher Spec TCP Acknowledgement attributes include TCP source port, TCP destination port, TCP source or destination port, TCP stream index, TCP segment length, TCP sequence number, TCP next sequence number, TCP acknowledgement number, TCP header length, reserved flag, nonce flag, congestion window reduced flag, ECN-echo flag, urgent flag, acknowledgement flag, push flag, reset flag, syn flag, fin flag, TCP window size value, calculated window size, window size scaling factor, checksum status, urgent pointer, NOP option, option kind, option length, timestamp value, time delta, and message order.

The TLS Alert attributes include TCP source port, TCP destination port, TCP source or destination port, TCP stream index, TCP segment length, TCP sequence number, TCP next sequence number, TCP acknowledgement number, TCP header length, reserved flag, nonce flag, congestion window reduced flag, ECN-echo flag, urgent flag, acknowledgement flag, push flag, reset flag, syn flag, fin flag, TCP window size value, calculated window size, window size scaling factor, checksum status, urgent pointer, NOP option, option kind, option length, timestamp value, time delta, TLS record content type, TLS record length, TLS alert message level, TLS alert message description, and message order.

The TCP Disconnect attributes include TCP source port, TCP destination port, TCP source or destination port, TCP stream index, TCP segment length, TCP sequence number, TCP next sequence number, TCP acknowledgement number, TCP header length, reserved flag, nonce flag, congestion window reduced flag, ECN-echo flag, urgent flag, acknowledgement flag, push flag, reset flag, syn flag, fin flag, TCP window size value, calculated window size, window size scaling factor, checksum status, urgent pointer, NOP option, option kind, option length, timestamp value, time delta, and message order.

Fold ID and Vulnerable Classes are also included in the dataset.

Features: Label, Change Cipher Spec TCP Acknowledgement attributes, TLS Alert attributes, TCP Disconnect attributes, Fold ID, and Vulnerable Classes.

Keywords: Bleichenbacher Timing Attack, TCP, TLS, Change Cipher Spec, TCP Acknowledgement, TLS Alert, TCP Disconnect, Fold ID, Vulnerable Classes.