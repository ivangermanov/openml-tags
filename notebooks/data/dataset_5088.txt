Timing Attack Dataset with 256 Micro Seconds Delay Created on September First Two Thousand Twenty-Two

Tags: Life Science, Machine Learning Bleichenbacher Timing Attack

Attribute Descriptions:

CCS Zero contains attributes related to the first Change Cipher Spec TCP Acknowledgement, including TCP source port, destination port, port, stream, segment length, sequence number, next sequence number, acknowledgement number, header length, reserved flag, nonce flag, congestion window reduced flag, ECN echo flag, urgent flag, acknowledgement flag, push flag, reset flag, syn flag, fin flag, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, options timestamp tsval, time delta, and message order.

TLS Zero contains attributes related to the first TLS Alert, including TCP source port, destination port, port, stream, segment length, sequence number, next sequence number, acknowledgement number, header length, reserved flag, nonce flag, congestion window reduced flag, ECN echo flag, urgent flag, acknowledgement flag, push flag, reset flag, syn flag, fin flag, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, options timestamp tsval, time delta, TLS record content type, TLS record length, TLS alert message level, TLS alert message description, and message order.

DISC Zero and DISC One contain attributes related to the first and second TCP Disconnect respectively, including TCP source port, destination port, port, stream, segment length, sequence number, next sequence number, acknowledgement number, header length, reserved flag, nonce flag, congestion window reduced flag, ECN echo flag, urgent flag, acknowledgement flag, push flag, reset flag, syn flag, fin flag, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, options timestamp tsval, time delta, and message order.

Features include label, CCS zero attributes, TLS zero attributes, DISC zero attributes, and DISC one attributes.

Keywords: Bleichenbacher Timing Attack, Machine Learning, Life Science, TCP, TLS, Change Cipher Spec, TCP Acknowledgement, TLS Alert, TCP Disconnect