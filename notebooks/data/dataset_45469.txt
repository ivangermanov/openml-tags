Timing Attack Dataset Sixty Four Micro Seconds Delay 

Tags: Bleichenbacher Timing Attack 

This dataset contains sixty-four microsecond delay data created on September Seventeenth Two Thousand Twenty-Two.

Attribute Descriptions:

The dataset includes attributes related to the first Change Cipher Spec TCP Acknowledgement, including the TCP source port, destination port, port, stream, segment length, sequence number, next sequence number, acknowledgment number, header length, flags, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options, kind, length, timestamp value, time delta, and message order.

Additionally, the dataset includes attributes related to the first TLS Alert, including the TCP source port, destination port, port, stream, segment length, sequence number, next sequence number, acknowledgment number, header length, flags, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options, kind, length, timestamp value, time delta, TLS record content type, TLS record length, TLS alert message level, TLS alert message description, and message order.

The dataset also includes attributes related to the first TCP Disconnect and second TCP Disconnect, including the TCP source port, destination port, port, stream, segment length, sequence number, next sequence number, acknowledgment number, header length, flags, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options, kind, length, timestamp value, time delta, and message order.

Features: 
label, TCP Source Port, TCP Destination Port, TCP Port, TCP Stream, TCP Segment Length, TCP Sequence Number, TCP Next Sequence Number, TCP Acknowledgment Number, TCP Header Length, TCP Flags, TCP Window Size Value, TCP Calculated Window Size, TCP Window Size Scale Factor, TCP Checksum Status, TCP Urgent Pointer, TCP Options, TCP Kind, TCP Length, TCP Timestamp Value, TCP Time Delta, TCP Message Order, TLS Record Content Type, TLS Record Length, TLS Alert Message Level, TLS Alert Message Description

Keywords: Timing Attack, Bleichenbacher, TCP, TLS, Dataset, Microseconds