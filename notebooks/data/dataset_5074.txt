Timing Attack Dataset - Eight Micro Seconds Delay - Created on Twelfth September Two Thousand Twenty-Two

Tags: Health, Machine Learning, Bleichenbacher Timing Attack

Attribute Descriptions:

The dataset contains attributes related to three types of messages: Change Cipher Spec, TLS Alert, and TCP Disconnect.

Change Cipher Spec attributes include:

* TCP source port
* TCP destination port
* TCP port
* TCP stream index
* TCP segment length
* TCP sequence number
* TCP next sequence number
* TCP acknowledgment number
* TCP header length
* TCP reserved flags
* TCP nonce flags
* TCP congestion window reduced flags
* TCP echo congestion notification flags
* TCP urgent flags
* TCP acknowledgment flags
* TCP push flags
* TCP reset flags
* TCP syn flags
* TCP fin flags
* TCP window size value
* TCP calculated window size
* TCP window size scale factor
* TCP checksum status
* TCP urgent pointer
* TCP options nop
* TCP option kind
* TCP option length
* TCP timestamp value
* Time delta since previous frame in this TCP stream
* Message order within the server responses

TLS Alert attributes include:

* TCP source port
* TCP destination port
* TCP port
* TCP stream index
* TCP segment length
* TCP sequence number
* TCP next sequence number
* TCP acknowledgment number
* TCP header length
* TCP reserved flags
* TCP nonce flags
* TCP congestion window reduced flags
* TCP echo congestion notification flags
* TCP urgent flags
* TCP acknowledgment flags
* TCP push flags
* TCP reset flags
* TCP syn flags
* TCP fin flags
* TCP window size value
* TCP calculated window size
* TCP window size scale factor
* TCP checksum status
* TCP urgent pointer
* TCP options nop
* TCP option kind
* TCP option length
* TCP timestamp value
* Time delta since previous frame in this TCP stream
* TLS record content type
* TLS record length
* TLS alert message level
* TLS alert message description
* Message order within the server responses

TCP Disconnect attributes include:

* TCP source port
* TCP destination port
* TCP port
* TCP stream index
* TCP segment length
* TCP sequence number
* TCP next sequence number
* TCP acknowledgment number
* TCP header length
* TCP reserved flags
* TCP nonce flags
* TCP congestion window reduced flags
* TCP echo congestion notification flags
* TCP urgent flags
* TCP acknowledgment flags
* TCP push flags
* TCP reset flags
* TCP syn flags
* TCP fin flags
* TCP window size value
* TCP calculated window size
* TCP window size scale factor
* TCP checksum status
* TCP urgent pointer
* TCP options nop
* TCP option kind
* TCP option length
* TCP timestamp value
* Time delta since previous frame in this TCP stream
* Message order within the server responses

Features:
Label,
Change Cipher Spec TCP Source Port,
Change Cipher Spec TCP Destination Port,
... (and many more)

Keywords: Bleichenbacher Timing Attack, Change Cipher Spec, TLS Alert, TCP Disconnect, Machine Learning, Health