Timing Attack Dataset: Thirty Microsecond Delay Created on Nineteen September Two Thousand Twenty-Two

Tags: Chemistry, Life Science Bleichenbacher 

This dataset contains attributes related to the Bleichenbacher Timing Attack with a thirty microsecond delay.

Attribute descriptions:

The Change Cipher Spec TCP Acknowledgement Attributes include:
TCP source port, TCP destination port, TCP source or destination port, TCP stream index, TCP segment length, TCP sequence number, TCP next sequence number, TCP acknowledgment number, TCP header length, reserved flag, nonce flag, congestion window reduced flag, ECN-echo flag, urgent flag, acknowledgment flag, push flag, reset flag, syn flag, fin flag, TCP window, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, timestamp value, time delta, message order.

The TLS Alert Attributes include:
TCP source port, TCP destination port, TCP source or destination port, TCP stream index, TCP segment length, TCP sequence number, TCP next sequence number, TCP acknowledgment number, TCP header length, reserved flag, nonce flag, congestion window reduced flag, ECN-echo flag, urgent flag, acknowledgment flag, push flag, reset flag, syn flag, fin flag, TCP window, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, timestamp value, time delta, TLS record content type, TLS record length, TLS alert message level, TLS alert message description, message order.

The TCP Disconnect Attributes include:
TCP source port, TCP destination port, TCP source or destination port, TCP stream index, TCP segment length, TCP sequence number, TCP next sequence number, TCP acknowledgment number, TCP header length, reserved flag, nonce flag, congestion window reduced flag, ECN-echo flag, urgent flag, acknowledgment flag, push flag, reset flag, syn flag, fin flag, TCP window, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, timestamp value, time delta, message order.

Features: 
label, 
Change Cipher Spec TCP acknowledgement attributes, 
TLS alert attributes, 
TCP disconnect attributes.

Keywords: Bleichenbacher, Timing Attack, TCP, TLS, Cryptography, Network Security.