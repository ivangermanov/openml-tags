Subsampling of the Dionis dataset with seed equal to zero arguments number rows two thousand arguments number columns one hundred arguments number classes ten arguments no stratify equal to true.

Generated with the following source code:

def subsample(self, seed: integer, number rows maximum equal to two thousand, 
number columns maximum equal to one hundred, number classes maximum equal to ten, 
stratified equal to true) -> Dataset:

random number generator equals numpy random default random generator seed
x equals self x
y equals self y

Uniformly sample classes equals y unique
if length of classes greater than number classes maximum:
value counts equals y value counts
selected classes equals random number generator choice classes size number classes maximum 
replace equal to false probability value counts divided by sum value counts 

Select the indices where one of these classes is present 
indices equals y index y is in selected classes
x equals x iloc indices
y equals y iloc indices

Uniformly sample columns if required
if length of x columns greater than number columns maximum:
column indices equals random number generator choice list range length x columns 
size number columns maximum replace equal to false
sorted column indices equals sorted column indices
selected columns equals list x columns sorted column indices
x equals x selected columns
else:
sorted column indices equals list range length x columns

if length of x greater than number rows maximum:
Stratify accordingly
target name equals y name
data equals pandas concat x y axis equal to columns
_, subset equals train test split data test size number rows maximum 
stratify data target name shuffle equal to true random state equal to seed
x equals subset drop target name axis equal to columns
y equals subset target name

We need to convert categorical columns to string for OpenML
categorical mask equals list self categorical mask i for i in sorted column indices
columns equals list x columns
return Dataset technically this is not the same but it's where it was derived from 
dataset equals self dataset x equals x y equals y 
categorical mask equals categorical mask columns equals columns

Tags: Subsampling of the Dionis dataset 

Features: 

Keywords: Dionis dataset, subsampling, stratification, OpenML, categorical columns.