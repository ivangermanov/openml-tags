Timing Attack Dataset 

Tags: Chemistry, Life Science 

Bleichenbacher Timing Attack 

This dataset was created on September Eighteenth, Two Thousand Twenty-Two, and consists of twenty microsecond delay data.

Attribute Descriptions:

Change Cipher Spec TCP Acknowledgement attributes include source port, destination port, source or destination port, TCP stream index, TCP segment length, TCP sequence number, next sequence number, acknowledgement number, header length, reserved flags, nonce flags, congestion window reduced flags, ECN echo flags, urgent flags, acknowledgement flags, push flags, reset flags, syn flags, fin flags, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, NOP options, option kind, option length, timestamp value, time delta, message order.

TLS Alert attributes include source port, destination port, source or destination port, TCP stream index, TCP segment length, TCP sequence number, next sequence number, acknowledgement number, header length, reserved flags, nonce flags, congestion window reduced flags, ECN echo flags, urgent flags, acknowledgement flags, push flags, reset flags, syn flags, fin flags, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, NOP options, option kind, option length, timestamp value, time delta, record content type, record length, alert message level, alert message description, message order.

TCP Disconnect attributes include source port, destination port, source or destination port, TCP stream index, TCP segment length, TCP sequence number, next sequence number, acknowledgement number, header length, reserved flags, nonce flags, congestion window reduced flags, ECN echo flags, urgent flags, acknowledgement flags, push flags, reset flags, syn flags, fin flags, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, NOP options, option kind, option length, timestamp value, time delta, message order.

Fold ID and vulnerable classes include PKCS one padding first eight bytes after zero zero, correctly formatted PKCS one PMS message but one byte shorter, no zero zero in message, wrong first byte zero zero set to seventeen, wrong second byte zero two set to seventeen.

Features: label, change cipher spec TCP acknowledgement attributes, TLS alert attributes, TCP disconnect attributes.

Keywords: Bleichenbacher Timing Attack, TCP Acknowledgement, TLS Alert, TCP Disconnect, PKCS one padding.