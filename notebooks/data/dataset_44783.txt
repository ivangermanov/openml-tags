Porto Seguro Seed Zero Dataset with Two Thousand Rows, Ten Classes, and One Hundred Columns, Stratified

This dataset is a subsample of the Porto Seguro dataset, generated with a seed of zero. The subsampling process involved uniformly sampling classes, columns, and rows to create a smaller dataset with specific characteristics.

The dataset has two thousand rows, ten classes, and one hundred columns. The stratified sampling method was used to ensure that the classes are represented proportionally in the subsample.

The features of this dataset include various variables related to individual and car characteristics, as well as calculated features. These features are a mix of categorical, binary, and numerical variables.

The target variable is not specified, but it is likely related to the prediction of a specific outcome or behavior.

Keywords: Porto Seguro, dataset, subsampling, stratified sampling, machine learning.