Timing Attack Dataset 

Tags: Chemistry, Life Science 

Bleichenbacher Timing Attack Dataset 

This dataset was created on September ninth, two thousand twenty-two, and contains information about twenty microsecond delays.

Attribute Descriptions:

The Change Cipher Spec TCP Acknowledgement section includes the following attributes: 
TCP source port, TCP destination port, TCP source or destination port, TCP stream index, TCP segment length, TCP sequence number, TCP next sequence number, TCP acknowledgement number, TCP header length, reserved flag, nonce flag, congestion window reduced flag, ECN echo flag, urgent flag, acknowledgement flag, push flag, reset flag, syn flag, fin flag, TCP window size value, calculated window size, window size scale factor, checksum status, urgent pointer, nop option, option kind, option length, timestamp value, time delta, and message order.

The TLS Alert section includes the following attributes: 
TCP source port, TCP destination port, TCP source or destination port, TCP stream index, TCP segment length, TCP sequence number, TCP next sequence number, TCP acknowledgement number, TCP header length, reserved flag, nonce flag, congestion window reduced flag, ECN echo flag, urgent flag, acknowledgement flag, push flag, reset flag, syn flag, fin flag, TCP window size value, calculated window size, window size scale factor, checksum status, urgent pointer, nop option, option kind, option length, timestamp value, time delta, TLS record content type, TLS record length, TLS alert message level, TLS alert message description, and message order.

The TCP Disconnect section includes the following attributes: 
TCP source port, TCP destination port, TCP source or destination port, TCP stream index, TCP segment length, TCP sequence number, TCP next sequence number, TCP acknowledgement number, TCP header length, reserved flag, nonce flag, congestion window reduced flag, ECN echo flag, urgent flag, acknowledgement flag, push flag, reset flag, syn flag, fin flag, TCP window size value, calculated window size, window size scale factor, checksum status, urgent pointer, nop option, option kind, option length, timestamp value, time delta, and message order.

Features: 
label, change cipher spec TCP source port, change cipher spec TCP destination port, and so on...

Vulnerable classes include incorrect PKCS one padding, correctly formatted PKCS one PMS message but one byte shorter, no zero in message, wrong first byte, and wrong second byte.

Keywords: Bleichenbacher Timing Attack, TCP, TLS, PKCS, Vulnerable Classes