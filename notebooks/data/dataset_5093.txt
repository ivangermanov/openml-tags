Timing Attack Dataset Created on September Ninth Two Thousand Twenty-Two

Tags: Bleichenbacher Timing Attack 

Attribute Descriptions:

CCS Zero contains attributes related to the First Change Cipher Spec TCP Acknowledgement, including TCP source port, destination port, source or destination port, TCP stream index, TCP segment length, TCP sequence number, next sequence number, acknowledgment number, header length, reserved flag, nonce flag, congestion window reduced flag, ECN echo flag, urgent flag, acknowledgment flag, push flag, reset flag, syn flag, fin flag, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, timestamp value, time delta, and message order.

TLS Zero contains attributes related to the First TLS Alert, including TCP source port, destination port, source or destination port, TCP stream index, TCP segment length, TCP sequence number, next sequence number, acknowledgment number, header length, reserved flag, nonce flag, congestion window reduced flag, ECN echo flag, urgent flag, acknowledgment flag, push flag, reset flag, syn flag, fin flag, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, timestamp value, time delta, TLS record content type, TLS record length, TLS alert message level, TLS alert message description, and message order.

DISC Zero and DISC One contain attributes related to the First and Second TCP Disconnect, respectively, including TCP source port, destination port, source or destination port, TCP stream index, TCP segment length, TCP sequence number, next sequence number, acknowledgment number, header length, reserved flag, nonce flag, congestion window reduced flag, ECN echo flag, urgent flag, acknowledgment flag, push flag, reset flag, syn flag, fin flag, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, timestamp value, time delta, and message order.

Features include label, CCS Zero attributes, TLS Zero attributes, DISC Zero attributes, and DISC One attributes.

Fold ID Two is associated with vulnerable classes, including PKCS one padding issues, incorrectly formatted PKCS one PMS messages, missing zero bytes in messages, wrong first byte, and wrong second byte.

Keywords: Bleichenbacher Timing Attack, TCP Attributes, TLS Attributes, Vulnerable Classes, PKCS One Padding Issues