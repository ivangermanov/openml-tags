Timing Attack Dataset Two Micro Seconds Delay 

Tags: Computer Systems, Machine Learning, Bleichenbacher Timing Attack 

This dataset contains two microsecond delay data created on September First, Two Thousand Twenty-Two.

Attribute Descriptions:

The dataset includes attributes related to the first Change Cipher Spec TCP Acknowledgement, such as TCP source port, destination port, port, stream, segment length, sequence number, next sequence number, acknowledgment number, header length, reserved flags, nonce flags, congestion window reduced flags, ECN echo flags, urgent flags, acknowledgment flags, push flags, reset flags, syn flags, fin flags, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, timestamp value, time delta, and message order.

Additionally, the dataset includes attributes related to the first TLS Alert, such as TCP source port, destination port, port, stream, segment length, sequence number, next sequence number, acknowledgment number, header length, reserved flags, nonce flags, congestion window reduced flags, ECN echo flags, urgent flags, acknowledgment flags, push flags, reset flags, syn flags, fin flags, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, timestamp value, time delta, TLS record content type, TLS record length, TLS alert message level, TLS alert message description, and message order.

Furthermore, the dataset includes attributes related to the first and second TCP Disconnect, such as TCP source port, destination port, port, stream, segment length, sequence number, next sequence number, acknowledgment number, header length, reserved flags, nonce flags, congestion window reduced flags, ECN echo flags, urgent flags, acknowledgment flags, push flags, reset flags, syn flags, fin flags, window size value, calculated window size, window size scale factor, checksum status, urgent pointer, options nop, option kind, option length, timestamp value, time delta, and message order.

Features: 
label, 
TCP Source Port, 
TCP Destination Port, 
TCP Port, 
TCP Stream, 
TCP Segment Length, 
TCP Sequence Number, 
TCP Next Sequence Number, 
TCP Acknowledgment Number, 
TCP Header Length, 
TCP Reserved Flags, 
TCP Nonce Flags, 
TCP Congestion Window Reduced Flags, 
TCP ECN Echo Flags, 
TCP Urgent Flags, 
TCP Acknowledgment Flags, 
TCP Push Flags, 
TCP Reset Flags, 
TCP Syn Flags, 
TCP Fin Flags, 
TCP Window Size Value, 
TCP Calculated Window Size, 
TCP Window Size Scale Factor, 
TCP Checksum Status, 
TCP Urgent Pointer, 
TCP Options Nop, 
TCP Option Kind, 
TCP Option Length, 
TCP Timestamp Value, 
TCP Time Delta, 
Message Order, 
TLS Record Content Type, 
TLS Record Length, 
TLS Alert Message Level, 
TLS Alert Message Description, 
and so on...

Keywords: Timing Attack, Bleichenbacher, Computer Systems, Machine Learning, TCP, TLS, Dataset.