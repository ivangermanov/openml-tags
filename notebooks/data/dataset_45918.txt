Padding Attack Dataset Cisco 

Tags: Bleichenbacher Padding Attack

This dataset was created on December fourth, two thousand twenty-three, with a server from Cisco.

Attribute Names:

The dataset includes attributes such as source port, destination port, port, stream, length, sequence number, next sequence number, acknowledgement number, header length, flags, window size value, window size, window size scale factor, checksum status, urgent pointer, options, and timestamp.

The dataset also includes attributes related to TLS records, such as content type, length, alert message level, and alert message description.

Vulnerable Classes:

The dataset includes vulnerable classes such as incorrect PKCS#1 padding, incorrect first byte, and incorrect second byte.

Features:

The dataset includes features such as source port, destination port, port, stream, length, sequence number, next sequence number, acknowledgement number, header length, flags, window size value, window size, window size scale factor, checksum status, urgent pointer, options, timestamp, and order.

Keywords: Bleichenbacher Padding Attack, Cisco, TLS Records, PKCS#1 Padding, Vulnerable Classes