Padding Attack Dataset Netscaler GCM 2023-12-13 Tags: Bleichenbacher Padding Attack

This dataset was created on December thirteenth, two thousand twenty-three, with server Netscaler GCM. It contains a collection of attributes, including TCP source port, TCP destination port, TCP port, TCP stream, TCP length, TCP sequence number, TCP next sequence number, TCP acknowledgement number, TCP header length, TCP flags, TCP window size value, TCP window size, TCP window size scale factor, TCP checksum status, TCP urgent pointer, TCP options, TCP timestamp, and TCP time delta. Additionally, it includes TLS record content type, TLS record length, TLS alert message level, and TLS alert message description.

The dataset also includes attributes from other categories, such as DISC, CCS, and TLS, which contain similar information about TCP and TLS protocols.

The vulnerable classes in this dataset include incorrect PKCS#1 padding, incorrect padding bytes, correctly formatted PKCS#1 PMS message but one byte shorter, no zero in message, wrong first byte, and wrong second byte.

Features: label, TCP source port, TCP destination port, TCP port, TCP stream, TCP length, TCP sequence number, TCP next sequence number, TCP acknowledgement number, TCP header length, TCP flags, TCP window size value, TCP window size, TCP window size scale factor, TCP checksum status, TCP urgent pointer, TCP options, TCP timestamp, TCP time delta, TLS record content type, TLS record length, TLS alert message level, TLS alert message description, and order.

Keywords: Bleichenbacher padding attack, Netscaler GCM, TCP protocol, TLS protocol, PKCS#1 padding, padding oracle attack.